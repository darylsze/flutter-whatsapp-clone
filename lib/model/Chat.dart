class Chat {
  final message, fromUser, toUser, createdAt, updatedAt, messageState;

  Chat({
    this.toUser,
    this.message,
    this.fromUser,
    this.createdAt,
    this.updatedAt,
    this.messageState,
  });
}
